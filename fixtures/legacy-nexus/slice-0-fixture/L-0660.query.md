---
ID: L-0660
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0658
Type: QUERY
To: architect
From: architect-L-0660
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-07-27
---

# L-0660 QUERY: cc-connect Session Transparency Ledger — Phase 1 Implementation (Boss-approved from L-0658 RFC)

## Context Digest

- (L-0658) RFC diagnosed six independent session/dispatch lifecycle decision sites in cc-connect and proposed a 5-phase `ControlPlane` + `sessions_ledger.json` migration. Phase 1 (visibility-only: ledger + `/sessions` command + `StatusBoard` extension) was scoped as low-risk and independently shippable, with Phases 2-5 deferred pending L-0657 (open, touches the same `core/dispatch.go`/`core/notify.go` files).
- Boss, in direct Telegram reply to the L-0658 RESULT (2026-07-27): "先批准 Phase 1 上线，Phase 2-5 待 L-0657 结案后再开后续信推进" — explicit, real-time authorization to build and ship Phase 1 now; Phases 2-5 explicitly deferred to a future letter after L-0657 closes.
- Engineer-originated per the lateral-branch path (protocol §Engineer-Originated `From:` Letter, L-0442 variant — reuses L-0658's existing Thread/Parent chain, not a new Thread, so the stricter new-Thread echo/dedup gates (L-0454) do not apply): 主闸 satisfied (Boss's instruction above, this turn); 结构闸 satisfied (L-0658's QUERY has no Engineer-originated `From:`, so this is a single hop).
- Archive scan (L-0636): rescanned `INDEX.md`/`threads/cc-connect-dispatch-architecture-redesign/` for "sessions_ledger", "/sessions command" — no prior letter already implements this; L-0658 is the only prior art (cited above), no duplicate found.

## Query

Implement Phase 1 exactly as scoped in L-0658's RFC, nothing more:

1. A new `sessions_ledger.json` store (same atomic-write/mutex-guarded shape as `dispatchStore` in `core/dispatch.go:119-236`) that records, per logical session, its `SessionID`, logical workspace, physical `AgentSessionID`, a state enum (active/idle/rotating/closed), bound Telegram topic (if any), and the timestamp/reason/actor of its last transition.
2. Wire the ledger to update at existing transition points ONLY (`resetSessionForTurn`, `/new`, `/switch`, dispatch creation) — read-only observation of transitions that already happen; do not change any existing transition's behavior or add new transition triggers (that is Phase 2+, explicitly deferred).
3. A `/sessions` command that renders the ledger's current contents (which physical sessions exist right now, for which workspace, in what state).
4. Extend `core/statusboard.go`'s rendered board with a per-session summary line drawn from the same ledger, alongside its existing per-engine working/idle/assigned/hung row (`renderTextLocked`, `core/statusboard.go:268-293`) — additive, not a replacement of the existing engine-level row.

## Expected Output

1. Code changes confined to a per-task worktree off `F:\GitHub\cc-connect` (Tier per BOSS_HANDBOOK §Git & PR Tiered Policy — no push/PR without a further explicit instruction, per Execute Constraints).
2. A passing test proving the ledger's `/sessions` output matches on-disk session-shard state 1:1 (same verification method as the L-0658-cited retro: hash/compare candidate workspace identities against actual shard files), plus `go test ./...` green.
3. Evidence in the RESULT: file paths, line numbers, test output — not claims. Explicitly confirm no existing transition behavior changed (Phase 1 is additive-only).
4. If scope creeps toward Phase 2 (adding a `ControlPlane` component or changing any transition's trigger condition) during implementation, stop and return STUCK rather than silently expanding scope (Out-of-Scope discipline, L-0644).
