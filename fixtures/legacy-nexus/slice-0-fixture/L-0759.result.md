---
ID: L-0759
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0662
Type: RESULT
To: secretary
From: architect-L-0759
Route: heavy
Project: cc-connect
Base-Repo: F:\GitHub\cc-connect
Date: 2026-08-02
Status: DONE
---

# L-0759 Result: 派发闸下沉 actuator——收编两个绕过点，验证检查移进 `executeDispatch`，`classifyVerification` 收紧到协议 PASS 格式

## Current Status

- **Status**: DONE
- **Summary**: 四项全部落地 + 总裁 pursuit 补做——验证闸下沉到 actuator（`executeDispatch` 内置，L-0757 放置）；outbox secretary 分支改走 `ControlPlaneDispatch` 恢复 sole-entry + 审计；preference-extract 自动派发删除改走 Outbox 常规卡片（模板补 `Verify: none` + 空 `Verified:`，L-0766 OP1 先决）；`classifyVerification` 收紧到协议 PASS wire 格式（`yes`/`done`/`ok`/`BLOCK`/任意非空全部被拒，`none` 豁免保留）；**pursuit：确认卡片失败即 fail-closed 拒绝派发，`dispatchWithCardFallback` 自动派发删除**（总裁裁决，见 Open Points 更正）。
- **Latest commit**: `65711486` @ branch `architect/L-0759`（含 pursuit；前序 `0d6f62da`；local only，未 push/merge/deploy）
- **Verification**: core 1489/1489 pass、platform/telegram 52/52 pass（基线 main 640f7260 core 1487）；`go vet ./core` 干净；`git diff --check` 干净；全量 `go test ./...` 其余包失败与基线逐包一致（既有 Windows 环境失败，非本信回归）。
- **Updated**: 2026-08-02

## Conclusion

L-0759 四项全部完成，另总裁 pursuit 补做一项：派发准入检查从 wrapper 下沉到 actuator（`executeDispatch` 内置、从信件自身 front matter 读 `Verify`/`Verified`、L-0757 放置），`classifyVerification` 判据从"非空即通行"收紧为协议 PASS wire 格式（与 `archive-querygate.ps1:60` 同语义），outbox secretary 分支改调 `ControlPlaneDispatch`（恢复 L-0662 sole-entry 并进审计账本，`查的也记账`），preference-extract 自动派发按总裁 Q9 删除并补 `Verify: none` 模板使登记（L-0766 OP1 先决）转绿；**pursuit（总裁裁决）**：确认卡片失败即 fail-closed 拒绝派发（`dispatchWithCardFallback` 自动派发删除，改走 `rejectDispatch` + `dispatch_rejected` 审计）。类闸 `TestExecuteDispatchCallSitesBoundToControlPlane` 拒绝未来任何直接调用 actuator 的新调用点。已扫描（L-0636）：本信直接承接 L-0662 Phase 2b 派发部分、L-0757 Placement 规则 cc-connect 侧首执、L-0719 `none` 豁免；并行的 L-0758 为 Archive 侧对应执行；L-0760（G2b）已由总裁 CLOSED 决定暂不做，本信未触碰。代码基线为当前 main `640f7260`（QUERY 所注 `b0fd367f` 之后的 3 个 merge 已含 L-0762 校验回路工作，所有行号已重定位并实测）。

## Prior-Art Survey

验证闸的候选实现位置与理由（本信属 hot-path 架构改动，Route heavy，须交代 Prior Art）：

1. **wrapper 侧检查（现状两处重复）**——`core/outbox.go:944`（`handleOutboxCommand`）+ `core/dispatch_confirm.go:207`（`ConfirmDispatch`）。**拒绝**：L-0757 明确"闸装 actuator 不装 wrapper"；这两处正是互补路径缺陷的现场（查的 outbox 不记账、记账的 dispatch.go 文本路径不查验证）。
2. **`dispatchRequest` 增 `Verify`/`Verified` 字段、调用方传入**——**拒绝**：闸强度依赖调用方诚实；未来新调用点漏传字段即绕过，等于 wrapper 检查换皮，不构成"机器会拒绝"的边界。
3. **`executeDispatch` 从信件自身 front matter 读取（选定）**——**采用**：与 `scanOutboxQueries`（`core/outbox.go:221`）种子 outbox ledger 同源，archive 为真相、调用方无法省略字段绕过；读文件而非 `outboxMu`，`handleOutboxCommand` 持 `outboxMu.Lock()` 调 `executeDispatch` 不会自锁（sync.Mutex 不可重入，cc-connect 既有故障模式）；PASS 语义与 Archive 侧 `archive-querygate.ps1:60` 单谓词对齐，两层共享同一语义。
4. 无新框架/库引入；`regexp` 为标准库（Go 1.26）。

## Observations & Invariants at Risk

- **观察（一手，基线 main @ `640f7260`）**：`grep -rn "executeDispatch(" core/`（排除定义）实测 3 处生产调用——`core/controlplane.go:24`（wrapper 自身）、`core/outbox.go:948`（`handleOutboxCommand` secretary 分支）、`core/engine.go:8152`（`extractPreferencesFromClosedLetter`）；`classifyVerification` @ `core/outbox.go:48` 判据为 `strings.TrimSpace(verified) != ""`（`:54`）；wrapper 验证检查 @ `core/outbox.go:944` 与 `core/dispatch_confirm.go:204-209`；`buildPreferenceExtractQuery` @ `core/preference_extract.go:60-89` 无 `Verify:`/`Verified:` 字段行。
- **风险不变量**：派发审计账本（`control_plane_audit.json`）与验证状态均由**稳定键**（letter ID / letter 文件路径 + front matter）派生，绝不从消息正文派生——本信未改变任何持久身份来源：闸从 `req.Path`（`validateDispatchArchive` 已验证 ID/Thread/Type 匹配的 letter 文件）读取，经 `classifyVerification` 判定。Cardinal Invariant 保持。
- **部署真相**：本信验证的是**源码合并**（worktree 内 build + test + 红证）；运行中 cc-connect binary 未重建/重启，本改动未上线。L-0762 的 `baf6bfa7` 亦为 local 未推送，本信叠加于其上。

## Gate Deposited

**类闸（primary）—— `TestExecuteDispatchCallSitesBoundToControlPlane`** @ `core/dispatch_test.go`；命令：`go test ./core/ -run TestExecuteDispatchCallSitesBoundToControlPlane -count=1`。

- **机器拒绝什么**：任何在 `ControlPlaneDispatch`（`core/controlplane.go:23`）之外直接以 `e.executeDispatch(` 调用 actuator 的新代码——即本信关闭的缺陷类（outbox.go:948 与 engine.go:8152 直调，绕过审计 + 验证）。扫描 `core/*.go`（排除 `_test.go`）的接收者限定调用表达式 `e.executeDispatch(`，唯一放行点是 `core/controlplane.go`（wrapper 自身调用 `:24`）。函数定义行 `func (e *Engine) executeDispatch(...)` 不匹配 `e.executeDispatch(` 模式，天然排除。
- **种类**：类闸（约束未来代码；拒绝整个缺陷类而非固定本信实例）。L-0697 覆盖声明见下。
- **红证（原始输出，基线 @ `640f7260`，代码已 stash 恢复修复前、仅留新测试）**：
  ```
  --- FAIL: TestExecuteDispatchCallSitesBoundToControlPlane (0.01s)
      dispatch_test.go:994: class gate: engine.go:8152 calls the actuator directly (e.executeDispatch) outside ControlPlaneDispatch — route the dispatch through ControlPlaneDispatch so it is audited and gated
  ```
  修复后同命令绿（`ok github.com/JayGarland/cc-connect/core`，见 Verification）。

**实例闸（item 3 行为）—— `TestExecuteDispatchActuatorVerificationGate`** @ `core/dispatch_test.go`：命名验证席 + 空 `Verified:` → 直接 `executeDispatch` 与经 `ControlPlaneDispatch` 均返回 `verification is awaiting`，且后者在审计账本留下 outcome 含该错误的条目（`查的也记账`）；协议 PASS wire 值放行；`none` 豁免放行。
- **红证（原始输出，基线 @ `640f7260`）**：
  ```
  --- FAIL: TestExecuteDispatchActuatorVerificationGate (0.02s)
      dispatch_test.go:928: direct executeDispatch with empty Verified: err = <nil>, want 'verification is awaiting'
  ```

**实例闸（item 4 语义）—— `TestVerificationStateUsesArchiveTextOnly`** 扩展 @ `core/outbox_test.go`：判据 = `verifiedPassWireRe`（`core/outbox.go:46`，`^\S+-L-\d{4}\s*·\s*\d{4}-\d{2}-\d{2}\s*·\s*PASS\s*$`，镜像 `archive-querygate.ps1:60`）。
- **红证（原始输出，基线 @ `640f7260`）**——修复前弱判据把下列值全部判 `ready`：
  ```
  --- FAIL: TestVerificationStateUsesArchiveTextOnly (0.00s)
      --- FAIL: .../named_verifier_awaits_yes
      --- FAIL: .../named_verifier_awaits_done
      --- FAIL: .../named_verifier_awaits_ok
      --- FAIL: .../named_verifier_awaits_BLOCK
      --- FAIL: .../named_verifier_awaits_arbitrary_text
      --- FAIL: .../named_verifier_awaits_bare_PASS_literal
      --- FAIL: .../protocol_PASS_wire_format_missing_date_is_awaiting
  ```
  修复后全部绿；协议 PASS 值（`architect-codex-L-0686 · 2026-07-29 · PASS`）与 `none` 豁免分支保持 ready（L-0719 未回归）。

**实例闸（item 1 先决 + Outbox 可见行为）—— `TestPreferenceExtractQueryAppearsInOutbox`** @ `core/dispatch_test.go`：真实 `buildPreferenceExtractQuery` 生成 → INDEX 登记行 → 真实 `scanOutboxQueries` 拾取为 `Verify: none` → `classifyVerification` = ready（secretary 派发按钮渲染）。配合 archive-querygate.ps1 红→绿（见下）。
- **红证（原始输出，基线 @ `640f7260`，模板缺字段）**：
  ```
  --- FAIL: TestPreferenceExtractQueryAppearsInOutbox (0.01s)
      dispatch_test.go:1045: preference-extract QUERY must carry the none exemption, got Verify=""
  ```
- **archive-querygate.ps1 实跑红→绿（同一模板前后对比）**：
  ```
  GATE THROW: QUERY header is missing the required 'Verify:' field (L-0687). Name the validating seat (architect-codex / qa-engineer / reviewer) or write 'none - <ground>'.
  GATE PASS
  ```

### Coverage declaration（L-0697 + L-0757 层次维度）

- **actuator**：`executeDispatch`（定义 @ `core/dispatch.go:670` @ `0d6f62da`）。闸装在 actuator 内（`:713-730`），不在任何 wrapper 上。
- **`executeDispatch` 全部生产调用点**（改动后，@ `0d6f62da`）：
  | 调用点 | 改动后是否经闸 |
  |---|---|
  | `core/controlplane.go:24`（`ControlPlaneDispatch` 内部唯一调用） | ✓ 经闸（gate 在 actuator 内） |
  | `core/outbox.go:960`（`handleOutboxCommand` secretary 分支） | ✓ 已改调 `ControlPlaneDispatch`（item 2），经闸 + 进审计 |
  | `core/engine.go:8152`（preference-extract） | ✓ 已删除（item 1），不再存在 |
  - **`ControlPlaneDispatch` 生产调用点**：`core/dispatch.go:623`（`dispatchWithCardFallback`，SendReceiptCard 失败降级自动派发）、`core/dispatch_confirm.go:218`（`ConfirmDispatch`）、`core/outbox.go:960`（本信新增）——全部最终到 `executeDispatch`，均经闸。
- **测试调用点**：`core/dispatch_test.go` 6 处直调 `executeDispatch`（`:353/:407/:478/:601/:710/:819`）——其 query 文件无 `Verify:`（空 → ready）→ 闸放行，测试保持绿；测试直调非生产派发路径，不构成绕过。
- **`args[0]` 枚举收窄处置**（既有 `core/outbox.go:944` 条件 `(args[0]=="manual" || args[0]=="secretary")`）：
  - 全取值：`page`、`collapse`、`manual`、`secretary`（`:931` 白名单；枚举外默认拒绝 `❌ Outbox item is unavailable`）。`page`/`collapse` 为纯卡片渲染（不派发），无需验证守卫；`manual`（不调 `executeDispatch`，见 Open Points）与 `secretary`（派发）需守卫。
  - **为何枚举收窄不再是缺陷**：本信将绑定执行点下沉到 actuator（`args[0]`-无关），`:944` 退化为 UX 快速提示而非执行边界——未来新增派发动作即使漏加 `:944` 枚举，actuator 闸依然拦截（任何派发必须经 `executeDispatch`）。按 L-0697，此为明示设计决定，非静默收窄。
- **实例闸为何未做类闸（item 4）**：`classifyVerification` 语义已由 Archive 侧 `archive-querygate.ps1:60` 作为类闸持有（登记即拒绝非 PASS 格式）；cc-connect 侧镜像同谓词保持跨层语义对齐（L-0758 单谓词原则），测试矩阵为实例回归闸，覆盖该语义两侧（Go + PS）不失配。

## Verification

- `go test ./core/ -count=1`：**1490 pass / 0 fail**（基线 main @ `640f7260` 同机实测 **1487**，净 +3 = 3 个新测试全部通过；`classifyVerification` 子测例矩阵由 5 → 12）。
- `go test ./platform/telegram/ -count=1`：**52 pass / 0 fail**。
- `go vet ./core`：干净。`git diff --check`：干净（仅 dispatch_test.go LF/CRLF 提示，无害）。
- 全量 `go test ./...`：其余包失败（`agent/claudecode`、`agent/cursor`、`agent/iflow`、`agent/kimi`、`agent/opencode`、`agent/pi`、`config`、`daemon`、`cmd/cc-connect` + `web` 因 `all:dist` 无前端构建产物 setup 失败）与基线 main @ `640f7260` **逐包一致**（规范化耗时后 `comm -13` 空集）——既有 Windows 环境失败（agent 模型发现 / config / PTY 路径等，cf. `cc-connect-dev` skill），**非本信回归**。

**三条派发路径行为逐一说明**：
- **[DISPATCH] 文本路径**（parse → 确认卡 → `ConfirmDispatch` → `ControlPlaneDispatch`）：流程**逐字节不变**；新增 actuator 闸（原先仅 `ConfirmDispatch` wrapper 检查），未验证信现被 actuator 层也拦截；审计本就覆盖。
- **Outbox 卡片路径**（`/outbox secretary`）：卡片渲染与按钮 callback **逐字节不变**（`core/outbox_test.go:225/:249` 断言 `cmd:/outbox secretary L-0100 g1` 保持）；secretary 动作改走 `ControlPlaneDispatch`（进审计 + 经闸）。`manual` 动作逐字节不变（不派发）。
- **confirm 卡片路径**：**逐字节不变**；actuator 闸为追加防线。

**preference-extract 删除自动派发后的实际可见行为实证**：
1. `extractPreferencesFromClosedLetter`（`core/engine.go:8104 @ 0d6f62da`）删除 `:8152` 的 `e.executeDispatch`，注册（`:8140` `registerArchiveQuery`）成功后回执 `MsgReceiptExtractPrefsRegistered`："QUERY %s registered. It is now in the Outbox — dispatch it from there when ready"（`core/i18n.go` 新消息）。
2. **Outbox 出现证据**：`TestPreferenceExtractQueryAppearsInOutbox` 用真实 `buildPreferenceExtractQuery` 生成 + INDEX 登记行 + 真实 `scanOutboxQueries`，断言 `L-0770` 被拾取且 `Verify: none` → ready（原始输出见 Gate Deposited）。边界说明：Outbox 扫描器（`scanOutboxQueries`，即 Outbox 卡片的数据源）为实证对象；完整 `archive-daily.ps1` git 提交步骤未在 scratch 根复跑（需 archive 根的 git/PS 栈），但登记失败的**唯一判据谓词**（`archive-querygate.ps1`）已直接红→绿实证——这正是 L-0766 OP1 定位的失败点。
3. 移除本身由类闸红证背书（基线 `engine.go:8152` 直调被闸点名 → 修复后该行不存在）。

## Open Points

- **`manual` 动作的产品语义（客观事实，未改动，Out-of-Scope 纪律）**：
  - 活持久状态：`outboxManual map[string]bool` @ `core/engine.go:532`；落盘 `data/outbox_manual.json`（path `core/outbox.go:1026`，save `:1051`，load `:1045`）；消费点 `core/outbox.go:613/:617/:652/:955-956/:1058-1064` 与 `core/delivery_ledger.go:314-322`（迁移进 delivery ledger）。
  - `manual` 分支 @ `core/outbox.go:954-959`：仅置 `outboxManual[letter]=true` + 持久化 + `markOutboxDispatched`，**不调 `executeDispatch`**——与 secretary 分支（派发）不同；验证守卫 `:944` 同样拦截 manual（awaiting 时）。
  - 原始提交 `6bae6366 feat(outbox): add boss and secretary dispatch actions`：按钮文本 `🙋 我自己发` → `manual`、`🧑💼 交秘书发` → `secretary`（commit 标题中的 "boss" 即 manual 按钮的语义，非字面 action key——`manual` 自始就是该 key，非由 `boss` 改名而来）。按钮仍渲染 @ `core/outbox.go:571`（ready 卡片）。
  - 总裁怀疑其孤儿性质：客观上它无派发副作用，仅把信件标记为"Boss 自己发"。是否仍是产品需要的动作，或应移除，属产品裁决，本信按纪律未顺手改动。
- **`dispatchWithCardFallback` 自动降级路径**（`core/dispatch.go:623`）：`SendReceiptCard` 失败时对 [DISPATCH] 提案**自动派发**（不经人工确认）。本信未改动（Scope 内无此要求），但 actuator 闸现亦拦截其未验证信；该自动降级是否应改为"失败而非自动派发"，值得单独评估（建议 Parent: L-0662）。
  > **更正 (2026-08-02)**: 总裁已裁决（L-0759 pursuit，Boss）并本信就地补做——确认卡片失败（平台无 `ReceiptCardManager` 能力或 `SendReceiptCard` 报错）即 fail-closed 拒绝派发：两个失败分支改走 `rejectDispatch`（slog + `dispatch_rejected` 审计条目 + 用户可见拒绝回执），`dispatchWithCardFallback` 函数已删除（`core/dispatch.go` @ `65711486`）。本 Open Point 已关闭。
- **G2b 可信 verifier 身份**：本信明确不做；并行的 L-0760（TRUSTED_IDENTITY_PARTIAL）已由总裁 CLOSED 裁决暂不做（信任模型=单人类，伪造风险可接受），本信不再重复。

## Options for Boss

1. **接受交付，走 Secretary 合入 `architect/L-0759` @ `0d6f62da`**（local 未推送，需 rebase origin/main 后 push）——常规路径，含类闸 + 四道红证 + 覆盖声明。
2. **先审核心四项 diff 再合入**——重点审 `core/dispatch.go:713-730`（actuator 闸）、`core/outbox.go:958-971`（secretary → ControlPlaneDispatch）、`core/outbox.go:46-71`（classifyVerification）、`core/preference_extract.go:73-78`（模板 Verify: none）。
3. **就 Open Points 另开子信**：`manual` 动作去留——`dispatchWithCardFallback` 已由总裁 pursuit 裁决并本信补做（fail-closed），不再待决。

<!-- Pursuit (2026-08-02): architect-L-0759 —— 总裁对 Open Point「dispatchWithCardFallback 自动降级路径」裁决：确认卡片发送失败时报失败（fail-closed），不得自动 executeDispatch 绕过总裁确认。本信就地补做：
- `core/dispatch.go`：`!supportsCard`（平台无 `ReceiptCardManager`）与 `SendReceiptCard` 报错两个分支改走 `rejectDispatch`（slog + `dispatch_rejected` 审计条目 + 拒绝回执）；`dispatchWithCardFallback` 函数删除。@ `65711486`。
- `core/dispatch_confirm_test.go`：三个旧 auto-fallback 测试移除，改两个 fail-closed 测试——`TestMaybeHandleDispatchBlock_NoCardCapabilityRejectsFailClosed` / `TestMaybeHandleDispatchBlock_CardSendFailureRejectsFailClosed`，断言拒绝 + 零派发 + 审计 `dispatch_rejected`。
- 红证：`0d6f62da` 上新测试红（`replacement:"✅ Dispatched L-0667 to dev-pro", want fail-closed rejection`）；`65711486` 上绿。
- 验证：core 1489/1489、platform/telegram 52/52、vet/diff 干净。 -->
