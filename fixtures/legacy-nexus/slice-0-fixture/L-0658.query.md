---
ID: L-0658
Thread: cc-connect-dispatch-architecture-redesign
Parent: ROOT
Type: QUERY
To: architect-claude
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\nexus
Date: 2026-07-26
---

# L-0658 QUERY: cc-connect 会话控制面与派工调度根源性架构诊断与重构 RFC

## Context Digest

- Boss feedback in conversation: `[DISPATCH]` text keyword matching in `core/dispatch.go` is merely a surface slice (symptom). The underlying root issue is that `cc-connect` ties session lifecycle, creation, switching, and cancellation unconditionally to text stream pattern matching without a deterministic control-plane state machine or explicit state ledger.
- This results in inconsistency: background sessions may be created, rotated, or switched implicitly upon matching text patterns, while the frontend user only sees plain text/prose without visibility into how many physical sessions exist or whether context was implicitly switched or canceled.
- (L-0386) Dispatch MVP implemented `[DISPATCH]` header regex parsing in `core/dispatch.go`.
- (L-0537) Task/Session tool vs Archive letter three-layer boundary.

## Query

1. **Root-Cause Structural Diagnosis**: Investigate the core engine in `cc-connect` to identify why session lifecycles, topic switching, and dispatching are triggered by text pattern matching instead of explicit control-plane primitives.
2. **Control/Data Decoupling RFC Design**: Design an RFC that completely decouples control flow from the text stream (e.g. replacing text regex matching with Telegram Native Inline Keyboard Buttons, explicit Tool Calls, or structured API endpoints).
3. **Session Transparency Ledger**: Propose a deterministic state-exposure mechanism so the frontend has 100% real-time visibility into physical session states (active, paused, created, closed).

## Expected Output

1. Outside-the-box structural diagnosis of `cc-connect` session control flow.
2. A formal RFC document with architecture diagrams and migration steps for decoupling control from text matching.
3. Explicit options for Boss decision.
