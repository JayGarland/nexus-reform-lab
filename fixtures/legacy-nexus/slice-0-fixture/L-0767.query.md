---
ID: L-0767
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0658
Type: QUERY
To: architect
From: architect-L-0658
Route: heavy
Verify: none — Boss 当轮明确豁免（「no need to ask pre dispatch」），本信由 architect 横向发起并自行执行
Verified:
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-08-03
---

# L-0767 QUERY: pattern seat 假信件工作区 fail-closed（路径 B：拒绝 + 大声申报）

## Context Digest

- (L-0658) RFC 立论：派工/会话决策应收束到单一控制面，"每次状态转移写一行台账"；本信是该 RFC 未覆盖的一条 fail-open 缺口。
- (L-0320) 当年为 `resolveWorkspacePattern` 加入 `messageHint`，允许在无 ledger 时从**消息正文**提取 letter ID；(L-0666) Phase 4 将其删除，理由是"持久绑定不得由易变消息内容决定"。删除是对的，但它使 `letterID = "L-" + threadID` 成为**唯一**兜底出口。
- (L-0702) 通过补全 dev-pro 的 `workspace_pattern` 解决派工工区阻断——dev-pro 自此成为 pattern seat，本缺口对其生效。
- **本轮一手实证（2026-08-03，基线 `65711486`）**：`core/engine.go:18343` 与 `:18307` 两处在 ledger miss + sticky miss 时执行 `letterID = "L-" + threadID`，捏造出不存在的信件号；`data/topic_letter_bindings.json` 记有 `{dev-pro, thread_id: 10565, letter: "L-10565"}`，而 `data/dispatch_expectations.json` 内既无 `10565` 亦无 `L-0765`（实测查无）。捏造值随即被 `bind()` 永久钉死。
- **假工作区确实被创建**：`F:\foundry\product\worktrees\L-10565` 存在，独立 `.git`（`gitdir: F:/GitHub/resonova/.git/worktrees/L-10565`），HEAD `1bdf32f`，非 junction/symlink（`LinkType` 为空）。
- **会话冷启动实证**：`F:\nexus\logs\cc-connect-stdout.log` `2026-08-03T05:47:06.840` — `session ID does not belong to this project, clearing it  session_key=F:\foundry\product\worktrees\L-10565:telegram:-1003917051393:10565:7664413698  invalid_session_id=e3bc2afd-…`，紧随 `is_resume=false`，丢弃前一轮（08-02 23:39→08-03 00:01）约 5.75 小时上下文。
- **落点错位的成因（fork CLI 侧，非本仓代码）**：`F:\nexus\.claude-dev-pro\.claude.json` 的 `projects."F:/GitHub/resonova".activeWorktreeSession` 记 `originalCwd: …\L-10565`、`worktreePath: …\L-0765`、`enteredExisting: true`；两个 transcript（`e3bc2afd`、`46759880`）均落在 `projects\F--foundry-product-worktrees-L-0765\`，其 JSONL 内 `cwd` 字段却为 `…\L-10565`；`projects\F--foundry-product-worktrees-L-10565` **不存在**。故 `ValidateSessionID`（`agent/claudecode/claudecode.go:492` → `validateSessionIDInProject:536` → `findProjectDir:1536`）按 cwd 派生键找不到目录 → 判假 → 清空。
- 该守卫本身是上游 `#599`（`30c7605f`）引入、并由 (L-0399) 适配 `CLAUDE_CONFIG_DIR`；**它不是缺陷，放宽它会重开跨项目 session 泄漏**，且违反 `observe-first-stable-identity.md` 的核心不变量。

## Query

pattern seat（`workspace_pattern` 含 `{{LETTER_ID}}`）在**无权威绑定**时，当前行为是捏造 `L-<threadID>` 并永久钉死——这是 fail-open。总裁裁决改为 **fail-closed 且不静默**（"失败后要关闭，而不是失败后开放；不要静默关闭，要大声说出来"），并明确选择**路径 B**：

1. **删除捏造**：`resolveWorkspacePattern` 在 ledger miss + sticky miss 时**不得**再产生 `L-<threadID>`。两个分支（`:18307` 空 pattern + `dispatchTopicIsolation`，`:18343` `{{LETTER_ID}}` pattern）都在范围内。
2. **拒绝而非降级**：不得沿用返回 `""` 的既有语义——因为 `resolveMessageWorkspaceContext`（`core/engine.go:18465`）会把 `""` 降级为 seat 的 global agent，而 dev-pro 的 `work_dir` 是 `F:\GitHub\resonova`（`config.toml:190`），即 **base repo 的 main worktree**，L-0564 明令禁止在此改/测/提交。把 agent 静默放进禁区，仍是静默失败，只是换了个地方流血。
3. **大声申报**：拒绝时必须产生**用户可见**的告知（Topic 内一条消息），说明此 topic 未经派发、pattern seat 拒绝进入任何工作区、正确做法是走派发卡片；同时留下结构化日志（含 project / thread_id / 拒绝原因）。沉默拒绝不算合规。

## Expected Output

**范围（严格）**：仅 `F:\GitHub\cc-connect` 的 workspace 解析与其消息路径降级行为 + 配套自动化闸门。基线 `65711486`，worktree `F:\foundry\architecture\worktrees\L-0767`，分支 `architect/L-0767`，local only，**不 push、不 merge、不重启生产 daemon**。

**交付物**：

1. 两个捏造分支改为 fail-closed；消息路径不得把"无权威绑定"降级到 seat 的 `work_dir`（pattern seat 语义下）。
2. 用户可见拒绝告知 + 结构化日志。
3. **`## Gate Deposited`（No-Gate-No-Done 强制）**：至少一道**类闸**，拒绝"未来任何在无权威绑定时产生非空 letter/workspace 的代码"，而非仅针对本次两处调用点。须附**变红原始输出**（改前 fail），并按 L-0697 给出**覆盖声明**（扫了哪些文件/函数、排除了哪些、每条排除为何成立），按 L-0757 声明**层次**（闸装在产生副作用的函数上，而非其 wrapper；列全到达该函数的调用点）。
4. **`## Observations & Invariants at Risk`（承重改动强制）**：本信改动的是持久身份派生，必须写明持久绑定来自哪个稳定键、以及 merge≠deploy 的部署真相。
5. **`## Evidence Gaps`（若有）**：已知一处——"cwd 对应的 project 目录是否存在"是否为冷启动的唯一开关，当前对照组仅 `L-0701`（n=1）。若该断言进入闸门判据，须在信内标注其证据强度，或先补验。

**不做（越界即违规，L-0644）**：不放宽 `ValidateSessionID`；不改 fork CLI 的 `activeWorktreeSession` 行为（该逻辑在编译后的二进制内，本仓无源码）；不改 `topic_letter_bindings.json` 既有数据（用可变编辑决定持久绑定，协议禁止）；不动 `config.toml` 的 seat `work_dir`。

**止血（总裁当轮批准，独立于上述代码交付）**：L-0765 走派发卡片正式 dispatch，使 workspace 与 fork 实际进入的 worktree 对齐，恢复 resume；此项不通过编辑绑定文件实现。

**预算**：无法在预算内收敛，或闸门无法建立时，交 STUCK 并列出卡点与 2–3 个处置选项，不伪装 DONE。
