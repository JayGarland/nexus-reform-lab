---
ID: L-0674
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0669
Type: QUERY
To: architect
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-07-27
Status: QUERY
---

# L-0674 QUERY: cc-connect Topic/Branch 命名反腐重构——彻底切除硬编码 `letter-` 前缀

## Context Digest

- **L-0658 (cc-connect 控制面重构 RFC)**：确立了控制面与数据面解耦、无硬编码抓取的原则。
- **L-0669 (Topic 属主自动路由)**：实现了基于派工账本的 Topic 属主识别，但代码中仍残留了多处硬编码的前缀拼接。
- **总裁硬性断言**：`L-XXXX`（如 `L-0669`）本身就已经具备完整且明确的信件语义表达能力，在外面强行包裹一层硬编码 `"letter-"` / `"letter/"` 前缀完全是多余、僵化且冗余的设计；“硬编码这个是瘤子”，必须彻底切除。

【秘书注/提醒】：本信所附的分析路径与解决方案仅作为秘书层的翻译与讨论参考，非硬性指令；执行席位（`architect`）须独立应用自身专业判断，设计并落地最终的实现方案。

## Query

请 `architect` 对 `cc-connect` 中涉及 Topic 命名与 Branch 命名的硬编码逻辑进行彻底切除与反腐重构：

1. **切除硬编码前缀瘤子**：彻底清理 `platform/telegram/telegram.go` 与 `core/dispatch.go` 等处硬编码拼接的 `"letter-new"`、`"letter-" + ID` 以及 `core/engine.go` 中的 `"letter/"` 分支前缀。
2. **干净命名派生**：充分利用 `L-XXXX` 自身的独立语义标识能力，或支持由任务上下文/配置干净派生，不再依赖写死的前缀字符串。
3. **兼容性与测试**：确保重构后全量测试 pass，且不破坏既有的派工账本查找与工作区创建逻辑。

## Expected Output

1. `cc-connect` 代码修改与单元测试，证明已彻底移除 `"letter-"`/`"letter/"` 硬编码前缀。
2. `go test ./core/... ./platform/telegram/...` 全量通过。
3. 预算：3 轮以内不收敛则交 STUCK。
