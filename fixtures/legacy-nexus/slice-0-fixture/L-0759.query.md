---
ID: L-0759
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0662
Type: QUERY
To: architect
From: architect-L-0759
Route: heavy
Verify: architect-codex
Verified: architect-codex-L-0759 · 2026-08-02 · PASS
Project: cc-connect
Base-Repo: F:\GitHub\cc-connect
Date: 2026-08-02
---

# L-0759 Query: 派发闸下沉 actuator——收编绕过 `ControlPlaneDispatch` 的两个调用点，验证检查移进 `executeDispatch`，`classifyVerification` 收紧到协议 PASS 格式

## Context Digest

- **L-0662 引入 `ControlPlaneDispatch`**（`core/controlplane.go:22`），其自身注释称之为 "the sole entry point for triggering a `[DISPATCH]` action"，包住 `executeDispatch`（`core/dispatch.go:670`）并在 `:24-25` 调用后写 `recordControlPlaneDispatch` 审计；该信 RESULT 明写 "session-lifecycle call sites deferred to Phase 2b"。**本信是 Phase 2b 的派发部分。**
- **实测该 sole-entry 不变量已被破坏**：`grep -rn "executeDispatch(" core/ | grep -v "func (e \*Engine)"` 得三个生产调用点——`core/controlplane.go:24`（wrapper 自身）、`core/outbox.go:627`（`handleOutboxCommand` secretary 分支）、`core/engine.go:8155`（`extractPreferencesFromClosedLetter` 内；基线 main @ `b0fd367f`）。**后两者直接够到 actuator，不经 `ControlPlaneDispatch`，因此不进审计账本。**
- **验证检查与审计落在互补路径**：唯一的验证前置检查在 `core/outbox.go:616`，它守的正是那条**绕过审计**的 `outbox.go:627`；而经 `ControlPlaneDispatch` 的 `[DISPATCH]` 文本路径（`core/dispatch.go:623`）**进审计但不查验证**。查的不记账，记账的不查。
- **`core/engine.go:8155` 是最严重的一条**：preference-extract 全自动——`extractPreferencesFromClosedLetter`（`:8107` @ main `b0fd367f`）内 `:8130` 分配 letter ID、`:8136` 写 QUERY、`:8143` 登记、`:8155` 直接 `executeDispatch` 派给 `reviewer`。**全程无人、无验证、无审计。**
- **总裁裁决（2026-08-02，当轮口述）**：preference-extract 这类自动派发**与人工派发同闸**，无豁免。按字面执行的结论是**删掉 `core/engine.go:8155`（`extractPreferencesFromClosedLetter` 内）的 `executeDispatch` 调用**——QUERY 登记后落进 Outbox 变成一张普通卡片，等总裁点，而不是给它单独加一道检查。**这一项是删代码，不是加代码。**
- **`classifyVerification` 判据过弱**：`core/outbox.go:41`，其自身注释（`:38-40`）自述 "performs no grammar, identity, authorship, or content validation"，实际判据是 `strings.TrimSpace(verified) != ""`——**任何非空字符串即通行**，而 `Verified:` 是 AI 可自行写入的 Markdown 字段。
- **L-0719 已修过同一函数的另一半**（`Verify` 值以 `none` 开头即判 ready，修复豁免信被误判为 awaiting）。**本信不得回归该修复。**
- **规则依据**：L-0757 已将「闸装在 actuator 上，绝不装在它的 wrapper 上」写入 BSE 协议 §No-Gate-No-Done，并要求覆盖声明含层次维度。本信是该规则在 cc-connect 侧的第一次执行；同一规则的 Archive 侧执行是并行的 L-0758。
- 已扫描 `INDEX.md` 与 `threads/`（关键词 `executeDispatch` / `ControlPlaneDispatch` / `sole entry` / `classifyVerification` / `preference-extract` / `outbox`）：命中 L-0658/L-0662（引入 + 明写延后）、L-0667（ConfirmDispatch 复用 ControlPlaneDispatch as sole actuator）、L-0711（两阶段 fail-closed 验证派发）、L-0717（intake 嵌入信体）、L-0719（`none` 豁免字面量）、L-0467（preference-extract 按钮复用 `executeDispatch` 的来源）。**未发现任何先例收编这两个绕过点。**

## Query

把派发准入检查从 wrapper 下沉到 actuator，使 `executeDispatch` 无论被谁调用都先过同一道闸，并让 `Verified:` 停止用"非空"当通行证。

四项，同一 chokepoint，按此顺序：

1. **`core/engine.go:8155`**（`extractPreferencesFromClosedLetter` 内的直接 `executeDispatch`）：按总裁 Q9 裁决删除自动派发调用，让 preference-extract QUERY 登记后走 Outbox 常规卡片路径。须确认删除后该 QUERY 确实出现在 Outbox（不是静默消失）。
2. **`core/outbox.go:627`**：改调 `ControlPlaneDispatch`，恢复 L-0662 的 sole-entry 不变量并使其进审计账本。
3. **验证检查下沉**：把 `core/outbox.go:616` 的前置检查移进 `executeDispatch`（或 `ControlPlaneDispatch`，二选一并说明理由），使所有调用点共用。
4. **`classifyVerification`（`core/outbox.go:41`）收紧**：判据改为**协议格式**——`Verified:` 须匹配 BSE 协议 wire 格式 `<seat>-<ID> · YYYY-MM-DD · PASS` 的 PASS 语义（语义基线：`F:\nexus\docs\protocols\Boss–Secretary–Engineer-protocol.md:68 @ e8a3cd4`），而非字面 `== "PASS"`，否则会误杀全部合规历史信。`none` 豁免分支（L-0719）必须保持。

**本信不做**：G2b 可信 verifier 身份（等并行的 L-0760 只读调查结论）；`admin_from` 收敛（总裁已明确本轮不做）。

## Expected Output

**前置**：**必须先 invoke `cc-connect-dev` skill**（本信触碰 session/routing/dispatch 热路径，非可选）。在 per-task worktree 执行，**禁止在 cc-connect 主 worktree 改测提交**。

**闸门要求（No-Gate-No-Done + L-0757）**：

- **红证**：每一项都要有"改前该路径能绕过 / 改后被拦"的原始输出。第 4 项须逐一证明下列值**全部被拒**：`yes`、`done`、`ok`、`BLOCK`、任意非空字符串；同时证明真实合规值（协议格式 PASS）与 `none - <依据>` 豁免**仍然通过**。
- **覆盖声明必须含层次维度（L-0757）**：列出 `executeDispatch` 的**全部**调用点（生产 + 测试分别标注），说明每一个在改动后是否经过闸。另须处理一个既有的枚举式收窄——`core/outbox.go:616` 现有条件是 `(args[0] == "manual" || args[0] == "secretary")`，**必须列全 `args[0]` 的所有取值**，说明枚举外的取值为何无需守卫，或改为默认拒绝。按 L-0697，静默的枚举收窄即缺陷。
- **闸的种类**：优先类闸（约束未来新增的调用点），例如禁止 `executeDispatch` 在 `ControlPlaneDispatch` 之外被调用的边界检查。

**验收标准**：

- Go 全量测试绿，给出计数；
- `[DISPATCH]` 文本路径、Outbox 卡片路径、confirm 卡片路径三者行为逐一说明（哪些变、哪些逐字节不变）；
- preference-extract 删除自动派发后的**实际可见行为**须实证（QUERY 出现在 Outbox 的证据），不接受"应该会"；
- 代码状态断言一律 `file:line` @ 具名 commit，**不得引用信件转述**。

**Open Points 必写**：`manual` 动作的产品语义。实测 `outboxManual` 是活的持久状态（落盘 `data/outbox_manual.json`，消费点 `core/outbox.go:321`、`:355`、`:718-724`），而其分支 `:617-619` **根本不调 `executeDispatch`**；原始提交 `6bae6366 feat(outbox): add boss and secretary dispatch actions` 显示当初两个动作叫 `boss` / `secretary`。总裁怀疑 `manual` 已是孤儿。**只客观摆事实写入 `## Open Points`，不得在本信顺手删除或改动它**（Out-of-Scope 纪律）。

**预算**：heavy。任一项无法在证据充分下收敛即交 STUCK，并说明已完成到哪一项。

<!-- 校验 (2026-08-02): architect-codex-L-0759 —— BLOCK: Citation re-verification 未通过。Context Digest 的核心执行点定位与当前基线不符：`core/outbox.go:627` 不是直接 `executeDispatch`，而是 receipt/err 分支；当前 Outbox fallback 调用的是 `ControlPlaneDispatch` 于 `core/outbox.go:623`（`executeDispatch` 的直接 Outbox 调用在 `handleOutboxCommand` 的 :627）。`core/engine.go:8190` 是 `sanitizeArchiveSummary` 的 return，preference-extract 的直接 `executeDispatch` 在 `:8155`（且 QUERY 所称 :8167/:8173 的 ID 分配/写 QUERY 亦不对应当前实现）。因此第 21、23 行及后续以这些行号为基的范围断言不可复核。另，`Verified:` wire 格式属于共享协议语义；第 39 行只称“协议格式”而未命名 BSE 协议的 committed `file:line @ commit` 基线，Semantic alignment 也未满足。Gate presence 通过：Expected Output :47-58 明确要求自动化 gate、红证与覆盖声明；但上述 BLOCK 足以令 Verified 保持为空。 -->

<!-- Correction (2026-08-02): secretary-L-0759 —— 应 architect-codex-L-0759 BLOCK；Boss 当轮指示 Secretary 代 architect 席修改。正文已改正，本注释记录核对事实与整改：

【Citation 整改】（基线 cc-connect main @ `b0fd367f` 实测）
- engine.go preference-extract 直接 `executeDispatch` 实为 **:8155**（非 :8190），所属函数 `extractPreferencesFromClosedLetter` 始于 :8107；信内子步骤 ID 分配 / 写 QUERY / 登记 实为 **:8130 / :8136 / :8143**（原 :8167/:8173/:8179 均偏高约 36 行，已逐一改正）。:8190 实为 `sanitizeArchiveSummary` 的 return，非本信改动对象。
- **outbox.go:627 经实测确为直接 `executeDispatch`**（`handleOutboxCommand` secretary 分支）——原引用**正确，予以保留**。outbox.go 全文件无任何 ControlPlaneDispatch；verifier 所称「outbox.go:623」实为 **dispatch.go:623**（[DISPATCH] 文本路径，本信第 22 行原引用即正确）。
- controlplane.go:24（wrapper 调用 + :25 recordControlPlaneDispatch）、dispatch.go:670（executeDispatch 定义）、outbox.go:616（验证前置检查）、outbox.go:41（classifyVerification）、dispatch_confirm.go:218 均已复核，与原引用一致。

【Semantic alignment】`Verified:` wire 格式语义基线 = `F:\nexus\docs\protocols\Boss–Secretary–Engineer-protocol.md:68 @ e8a3cd4`（`<seat>-<ID> · YYYY-MM-DD · PASS`）；已写入 Query 第 4 项。

本 correction 由 Secretary 依 Boss 当轮指示应用，设计意图与 To/Verify/基线未改；请 architect-codex-L-0759 复核后更新 Verified。 -->

<!-- 校验 (2026-08-02): architect-codex-L-0759 —— BLOCK: Correction 修复了 Context Digest 第 21/23 行和 Query 第 36/39 行的定位，BSE wire-format semantic baseline 亦已具名；Gate presence 通过。仍有一个 citation/consistency finding：Context Digest 第 24 行仍命令删除 `:8190` 的 `executeDispatch` 调用，但 correction 已确认 `core/engine.go:8190` 是 `sanitizeArchiveSummary` return，真实目标为 `:8155`；这使同一 QUERY 的实施对象自相矛盾，不能派发。请以追加 correction 将第 24 行的实施定位收敛到 `extractPreferencesFromClosedLetter():8155 @ b0fd367f`（或等价地移除过时 `:8190` 引用）后重新校验。Verified 保持为空。 -->

<!-- Correction v2 (2026-08-02): secretary-L-0759 —— 应 architect-codex-L-0759 BLOCK（第 24 行残留 :8190），Boss 当轮指示。第 24 行总裁裁决段的实施目标已收敛：

- 原「删掉 `:8190` 的 `executeDispatch` 调用」→ 改为「删掉 `core/engine.go:8155`（`extractPreferencesFromClosedLetter` 内）的 `executeDispatch` 调用」。全信正文已无过期 `:8190`/`:8167`/`:8173`/`:8179` 引用（Correction v1 第 69 行的历史记录除外，保留作审计）。
- 实施目标与第 23 行、Query 第 36 项（item 1）完全一致，均指向 `:8155 @ b0fd367f`。

本 correction v2 由 Secretary 依 Boss 当轮指示应用；请 architect-codex-L-0759 复核后更新 Verified。 -->

<!-- 校验 (2026-08-02): architect-codex-L-0759 —— PASS：Correction v2 已把第 24 行的自动派发删除目标收敛为 `extractPreferencesFromClosedLetter():8155 @ b0fd367f`，与 Context Digest 第 23 行及 Query item 1 一致；`core/engine.go:8130-8155` 复核了 ID 分配、QUERY 写入、登记与直接派发的完整序列。此前已通过的 sole-entry 调用点定位、BSE `Verified:` wire-format semantic baseline（`Boss–Secretary–Engineer-protocol.md:68 @ e8a3cd4`）与 gate presence 保持有效。 -->

<!-- Pursuit (2026-08-02): Secretary —— 总裁补充任务说明（L-0766 OP1，Boss 授权追加；不改变已验证正文，验证 PASS 保持有效）。

【preference-extract 模板修复，item 1 验收先决】`buildPreferenceExtractQuery`（`core/preference_extract.go:60-89 @ 7932152c`）生成的 QUERY **缺 `Verify:`/`Verified:` 字段行**，而 `archive-daily.ps1:91-106`（L-0687/L-0747）对 QUERY 缺字段必然 throw——preference-extract 登记自 2026-07-29（b1d664b）起**必失败**（最后成功登记 L-0604）。item 1「删除自动派发后确认 QUERY 出现在 Outbox」的**先决**：在 QUERY 模板补 `Verify: none - <依据>` + 空 `Verified:` 行（豁免流，无质检，与它全自动、零决策的性质一致）。 -->

<!-- Pursuit (2026-08-02): Boss —— Open Point「dispatchWithCardFallback 自动降级路径」裁定：确认卡片发送失败时报失败（fail-closed），不得自动 executeDispatch 绕过总裁确认。请在本信内补做（core/dispatch.go:623 的 SendReceiptCard 失败分支改为返回错误，不做自动派发），补做后更新 RESULT。 -->
