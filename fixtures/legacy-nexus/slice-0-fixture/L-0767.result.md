---
ID: L-0767
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0658
Type: RESULT
To: secretary
From: architect-L-0767
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-08-03

Status: DONE
---

## Current Status

- **Status**: DONE
- **Summary**: fail-closed 交付完毕；`resolveWorkspacePatternChecked` 是唯一生产分支，捏造路径已删除，拒绝立即大声回Telegram
- **Latest commit**: `9e433f2a`（分支 `architect/L-0767`，local only，未 push/merge）
- **Verification**: `go test ./core/` — 预期失败仅 `TestCmdShell_MultiWorkspaceIgnoresMissingSharedBinding`（基线 `65711486` 上同等失败，实证已取），新测试 5/5 绿，既有类闸 `TestMessageRouting_HasOneDecider` / `TestResolveWorkspacePattern_HasOneCaller` 绿
- **Updated**: 2026-08-03

---

## Conclusion

`"L-" + threadID` 捏造分支已删除。无派发权限的 pattern-seat topic 现在触发 `*unauthorizedTopicError`，在 WARN 日志打印后作为 Telegram 消息直接回复给用户，agent 不启动。`dispatchTopicIsolation` 无 pattern 的分支（session shard key）维持不变——threadID 本身是稳定键，不属于缺陷类。

已扫描归档，未发现语义相关先例（L-0666 删除文本抓取，L-0331 空 threadID 门卫，L-0702 workspace_pattern 补全——均已复核，方向一致）。

---

## Gate Deposited

**门类型**: class gate（拒绝整个捏造缺陷类）

**该闸拒绝的行为**: 任何向 `resolveWorkspacePatternChecked` 传入无权威绑定（无 ledger、无 sticky binding）的 threadID 并期望得到非错误返回值的调用——即"捏造 `L-<threadID>` 工作区"这整个行为类。

**位置**: `core/workspace_failclosed_test.go`
**运行命令**: `go test ./core/ -run "PatternSeat"`

**扫描范围**:
- 扫描了 `core/engine.go` 中 `resolveWorkspacePatternChecked` 的全部生产调用路径（4 个调用点：`topicWorkspaceKeyChecked`、`topicWorkspaceKey` wrapper、消息路径 line 18465、session 路径 line 18535）
- `dispatchTopicIsolation` 无 pattern 分支（`defaultDispatchWorkspaceKey "general"` 及 `"L-"+threadID` shard key）被有意排除在外：threadID 是平台分配的稳定键，非消息正文派生，不属本缺陷类
- 排除依据在 `core/engine.go:18242` 注释与 `testDispatchTopicIsolationShardBehavior` 测试中显式声明

**闸安装在 actuator**（`resolveWorkspacePatternChecked`），而非其 wrapper（`topicWorkspaceKeyChecked`/`topicWorkspaceKey`）。既有 `TestMessageRouting_HasOneDecider` 类闸已更新口径以覆盖新 actuator 名，原闸语义不变且更严格。

**变红证据（pre-fix）**:
```
# go build ./core/ （pre-fix，添加测试后）
core/workspace_failclosed_test.go:23:23: undefined: unauthorizedTopicError
FAIL    github.com/JayGarland/cc-connect/core [build failed]
```

**变绿证据（post-fix）**:
```
$ go test ./core/ -run "PatternSeat" -v
--- PASS: TestPatternSeatRefusesWithoutLedgerOrBinding (0.00s)
--- PASS: TestPatternSeatRefusalDoesNotPinBinding (0.00s)
--- PASS: TestPatternSeatStillAcceptsLedgerRegisteredTopic (0.00s)
--- PASS: TestPatternSeatHonorsStickyBindingSetByLedger (0.00s)
--- PASS: TestDispatchTopicIsolationSeatStillUsesThreadIDShard (0.00s)
ok      github.com/JayGarland/cc-connect/core
```

**Baseline**: `65711486`（`origin/main`）

---

## Evidence Gaps

**缺口**: 为什么 fork 的 `activeWorktreeSession` 在进程 cwd=`L-10565` 时将项目身份重定位至 `L-0765`（即"选 L-0765 的确切规则"）。

**现有证据**: `enteredExisting: true`，L-0765 当日有活跃 commit 记录（20:36/23:32），机制是"进入 repo 已登记的 active worktree"，但选择启发式在编译后的 claude 二进制中，不在本仓读取范围内。

**采用的临时判断**: 该缺口不影响 cc-connect 侧的修复正确性——关闭捏造路径后，fork 重定位与 cwd 不匹配的情景将改变（走派发卡片时 cwd=L-0765 = fork 的 active worktree，三身份对齐），session 不再丢失。

**Boss 需知**: 若 fork 的重定位规则改变，或将来有另一场景触发错位，cc-connect 侧可能需要进一步对齐（P1 路径：cc-connect 读 `.claude.json` `activeWorktreeSession` 确认 fork 实际进入的目录）。

---

## Options for Boss

1. **push 当前分支 + 合并（推荐）**: `git push origin architect/L-0767`，Secretary 或 Boss 走正常 merge 门禁。止血（topic 10565→L-0765 派发绑定）同时完成：走派发卡片正式 dispatch L-0765，三身份将自然对齐。
2. **先止血再 push**: 不等 merge，先在 topic 10565 点一次 dispatch 卡片派发 L-0765——cwd 与 fork active worktree 对齐，session resume 立即恢复。代码 merge 可后续进行。
3. **不处理止血，只 merge 代码**: 选项 3。已结束的 L-0765 任务不再需要止血；新的 pattern-seat topic 从此不会再被捏造。

## Open Points

- **fork cwd 对齐（P1，选项 1 后可观察）**: merge 并重启 cc-connect 后，观察下一次 pattern-seat dispatch 的 transcript 落点是否与 cwd 一致。若不一致，需立信调查"cc-connect 是否应读 `.claude.json` 以对齐 cwd 与 fork 实际 worktree"（Parent 建议本信）。
- **既有 Windows 路径测试（P2，scope 外）**: `TestCmdShell_MultiWorkspace*` 在 Windows 上因路径截断显示失败，基线 `65711486` 同等失败，与本改动无关。单独立信处理。
