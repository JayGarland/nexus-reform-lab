---
ID: L-0662
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0658
Type: QUERY
To: architect
From: architect-L-0662
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-07-27
---

# L-0662 QUERY: cc-connect Control-Plane Phase 2 (narrow slice) — `ControlPlane.Dispatch` Indirection + Audit Log

## Context Digest

- (L-0658) RFC Phase 2: introduce a `ControlPlane` component as the single call site for dispatch/session decisions; `parseDispatchBlock` becomes one *caller* of `ControlPlane.Dispatch`, not the decision-maker; "every transition writes one ledger row."
- (L-0660) Phase 1 shipped as a **live snapshot** (`/sessionboard`), not a persisted ledger — corrects L-0658's assumption that Phase 2 would extend a Phase-1 ledger file. Phase 2's audit trail is therefore a log owned by the sole decision-maker (`ControlPlane`) about its own actions, not a mirror of `SessionManager` state someone else owns — this does not reintroduce the "second source of truth" risk Phase 1 avoided, since a component logging its own decision is the primary record of that decision, not a copy of one.
- Boss, direct Telegram reply, this conversation, 2026-07-27: "approve 660 please continue phase 2" — 主闸 satisfied (explicit, this turn).
- **Structural-gate note**: this letter's `Parent` is `L-0658`, not `L-0660`. L-0660's QUERY was itself Engineer-originated (`From: architect-L-0660`); the protocol's single-hop rule (§Engineer-Originated `From:` Letter) forbids branching from a branch. `L-0662` branches from `L-0658` (secretary-originated, no Engineer-originated `From:`) directly — a second *independent* hop off the same non-Engineer-originated root, not a second hop stacked on `L-0660`.
- **Correction to my own L-0658 RESULT**: I told Boss Phase 2+ should wait for L-0657 because "both touch `core/dispatch.go`/`core/notify.go`." Re-verified before writing this letter: `git diff origin/main..architect/L-0654-ledger-race-fix --stat` shows that unpushed branch touches only `core/notify.go` + its test — not `core/dispatch.go`. The Phase-2 slice scoped below touches only `core/dispatch.go` + a new `core/controlplane.go`, not `core/notify.go`. The file-overlap risk I flagged in L-0658 does not materialize for this specific slice; Boss is proceeding on this corrected understanding, not the original overstated one.
- Transparency note (protocol spirit — this stays within one Thread's Parent chain, not a new-Thread direct dispatch, so the letter-of-the-rule dedup/echo gates for L-0454 don't strictly apply, but the underlying caution does): this is the second Engineer-originated letter I have self-registered in this conversation (L-0660, now L-0662). Flagging in case Boss/Secretary would prefer future phases route through the normal Secretary queue instead of continued lateral self-registration.
- Archive scan (L-0636): rescanned `INDEX.md`/`threads/cc-connect-dispatch-architecture-redesign/` — no prior letter implements a `ControlPlane` component; L-0658/L-0660 are the only prior art, both cited above.

## Query

Implement the **narrowest safe slice** of L-0658's Phase 2, not the full phase:

1. Add a `ControlPlane.Dispatch` method (new `core/controlplane.go`) that performs exactly `executeDispatch`'s existing logic (behavior-neutral — move or wrap, do not rewrite), and additionally appends one audit-log entry recording the decision (letter, thread, to, source project/session, timestamp, outcome).
2. `maybeHandleDispatchBlock` (`core/dispatch.go:487-504`) calls the new method instead of calling `e.executeDispatch` directly, becoming a caller of the control plane rather than the decision-maker itself.
3. **Explicitly deferred, not part of this letter**: `resetSessionForTurn`, `cmdNew`, `cmdSwitch`, or any other per-message session-lifecycle call site. Those sit on the actual hot path (every message, not just `[DISPATCH]` blocks) and carry materially higher blast-radius risk if a refactor introduces a subtle bug — bundling them into this letter would violate "smallest diff" and this letter's own risk judgment. They become a follow-up "Phase 2b" letter once this narrower slice is proven safe in practice.

## Expected Output

1. `core/controlplane.go`: `ControlPlane.Dispatch` wrapping `executeDispatch`'s existing body via minimal indirection, plus an append-only, atomic-write audit log (same shape as `dispatchStore`, `core/dispatch.go:119-236`) — call it `core/controlplane_audit.go` or fold into the same file.
2. `maybeHandleDispatchBlock` updated to call through the new method; `executeDispatch`'s own behavior for success/failure paths is byte-for-byte unchanged.
3. Passing tests: existing `core/dispatch_test.go` suite green with no modifications required by this change; a new test asserting exactly one audit-log entry per successful dispatch, with the fields named above.
4. `go vet`/`go build`/`go test ./core/... ./platform/telegram/...` clean. Work confined to a per-task worktree off `F:\GitHub\cc-connect`; no push/PR without further explicit instruction (Execute Constraints).
5. If achieving "no behavior change" turns out to require touching any session-lifecycle call site too, stop and return STUCK rather than silently absorbing Phase 2b into this letter (Out-of-Scope discipline, L-0644).
