---
ID: L-0674
Thread: cc-connect-dispatch-architecture-redesign
Parent: L-0669
Type: RESULT
To: secretary
From: architect-L-0674
Route: heavy
Project: nexus-toolchain
Base-Repo: F:\GitHub\cc-connect
Date: 2026-07-27
Status: DONE
---

## Conclusion

硬编码的 `"letter/"` / `"letter-"` 命名瘤子已从所有**生成**点彻底切除：worktree 分支名现在直接就是 `L-XXXX`（`L-XXXX` 本身已具备完整信件语义，外层前缀确属冗余），Topic 标题改为纯装饰性的 letter-id / 中性占位符。改动落在 per-task worktree `F:\foundry\architecture\worktrees\letter-L-0674`（branch `letter/L-0674`，commit `1f8413e6`，基于 `main` `cac8b91a`）；`go test ./core/... ./platform/telegram/...` 全绿。

<!-- 追击 (2026-07-27): Boss —— 进一步废除闲聊 Topic 的 new-task 占位符，General @ 创建 Topic 时一步到位用输入文本 slug 命名，删除第二次 EditForumTopic。已落地 commit 1d257d0a，见文末「追击」小节。 -->


## Observations & Invariants at Risk（承重改动前置）

- **观察（一手证据）**：
  - `core/engine.go:18187 branchNameForWorkspace` 生成分支名，喂给 `core/engine.go:4413-4429` 的 `git worktree add [--track origin/<branch>]` 与两处 prune 匹配器（`engine.go:19069`、CLI `cmd/cc-connect/worktree.go:195`）。这是本信唯一承重面。
  - **Topic 标题是装饰性的，非身份键**：路由身份全程走 `TopicID` / session-key / dispatch ledger（`findLetterIDByTopic` `engine.go:18000`、`TopicName` 仅作 `dispatch.go:680` 的展示元数据）。`dispatch.go:451/623`、`telegram.go:831/932` 的名字串从不被反解析。→ 可自由改名，零身份风险。
  - `git worktree list` 实测：在飞 worktree（`letter/L-0577/0669/0671/0674`）已存在于磁盘；创建代码仅在 `os.Stat(workspace)` NotExist 时运行（`engine.go:4365`），故它们不会被重建，分支名变更只影响**未来**信件。
- **风险不变量（稳定键，Cardinal Invariant）**：分支身份派生自 `extractLetterIDFromPath` 抽出的 `LETTER_ID`，而该 id 来自 ledger / sticky binding（L-0666/L-0669 已把它钉死为结构化来源，永不来自消息正文）。`"letter/"` 前缀只是套在**已稳定的键**外面的一层命名包装——切除它不触碰任何身份派生路径。诚实答案：稳定键 = ledger 的 `exp.Letter`，不是消息文本。✓
- **唯一跨新旧的路径 = prune**：给两处 prune 匹配器加 `isLetterWorktreeBranch(branch, letterID)`，同时识别新裸形 `L-XXXX` **与**遗留 `letter/L-XXXX`（磁盘上的 L-0577 等在飞 worktree），保证旧 worktree 仍能被正确回收。
- **部署真相**：验证的是**源码合并前的 worktree 构建 + 单测**，非运行 binary。`go build ./cmd/...` 因 `web/embed.go` 的 `all:dist` 资源缺失而失败（预存问题，需先 `npm run build`）；用临时 `web/dist` stub 验证 `cmd/cc-connect`（含 `worktree.go`）可独立编译通过，stub 已删除。**未 push、未 restart daemon**——落地到运行舰队需 Boss/Secretary 批准后 rebuild+restart。

## Prior-Art / Archive Scan

已扫描 `INDEX.md` 与 `threads/cc-connect-dispatch-architecture-redesign/`（关键词：letter-、prefix、命名、branch、topic、worktree、硬编码）。命中同线先例：**L-0658**（控制面 RFC，确立无硬编码抓取原则）、**L-0666**（Phase 4 彻底删除模式席位 L-XXXX 自由文本抓取）、**L-0669**（纯结构化账本绑定 Topic 属主）。本次改动与该链方向一致——三者把**身份**钉死为账本/稳定键；本信只切除包在稳定键外的**装饰性命名前缀**，不引入任何新的身份派生。未发现要求保留 `letter/` 前缀的相反先例。

## Changes（commit `1f8413e6`，6 files / +55 -24）

1. `core/engine.go::branchNameForWorkspace`：`letter/L-XXXX → L-XXXX`；thread 兜底 `letter-<id> → task-<id>`（`task-` 已被 `isThreadWorktreeBranch` 识别，语义更准）。删除 `dispatchLetterRe` 分支判断。
2. `core/engine.go` + `cmd/cc-connect/worktree.go`：新增 `isLetterWorktreeBranch(branch, letterID)`，prune 匹配同时接受裸形与遗留 `letter/` 形；保留 `isThreadWorktreeBranch` 的 `letter-`/`letter/` 前缀识别（**向后兼容磁盘上的遗留 worktree**，非新增瘤子）。
3. `core/dispatch.go`：虚拟 Topic 标题 `letter-<num> → letter-id 本身`；`CreateTaskTopic` 标题实参 `"letter-"+req.Letter → req.Letter`。
4. `platform/telegram/telegram.go`：新增常量 `newTopicPlaceholderName = "new-task"`，替换两处 `"letter-new"` 硬编码（装饰性占位符）。
5. 单测更新：`workspace_pattern_test.go` 分支期望改为裸 `L-XXXX`；`telegram_test.go` Topic 名期望改为 `new-task` / `new-task-build-the-thing`。

## Verification

- `go test ./core/... ./platform/telegram/...` → 两包 `ok`（core 30.5s、telegram 1.1s）。
- `go vet ./core/ ./platform/telegram/` → clean。
- `cmd/cc-connect`（含 `worktree.go`）在临时 web/dist stub 下编译通过。
- 残留 `letter-`/`letter/` grep：仅剩两处兼容识别器（`isThreadWorktreeBranch`、`isLetterWorktreeBranch` 遗留分支）——刻意保留，用于回收 L-0674 之前生成的磁盘 worktree。

## Open Points

- **在途 worktree 分支名不迁移**：磁盘上现存 `letter/L-0577/0669/0671/0674` 保持旧名（重命名活跃分支风险大、无收益）。它们随任务结束由 prune 的兼容分支自然回收；无需动作。若 Boss 希望统一历史命名，另开新信。
- **未 push / 未 PR / 未 restart**：本信只交源码+单测。是否 push、开 PR、rebuild+restart daemon 由 Boss/Secretary 决定（merge/push 保护分支的最终点击权属 Boss/Secretary）。
- **`web` embed 预存构建缺口**：`go build ./...` 因 `web/dist` 资源未构建而失败，与本改动无关；跑全量 `cmd` 构建前需先 `npm run build`（cc-connect-dev skill 已记录）。

---

## 追击 (2026-07-27): 闲聊 Topic 一步到位命名，废除 new-task 占位符（commit `1d257d0a`）

**背景**：Boss 指示在 L-0674 基础上继续切除闲聊 Topic 的 `new-task` 前缀——General @ 创建 Topic 时直接取输入文本前几个字（`topicTitleSlug`）作为初始标题一步创建，彻底废除占位符与第二次 `EditForumTopic` 调用。

**改动**（`platform/telegram/telegram.go`，`handleGeneralTopicIntake`）：
- 原流程：先用 `newTopicPlaceholderName`（`"new-task"`）创建 Topic → 再 `EditForumTopic` 追加 `-<slug>`（两次 API 往返）。
- 新流程：**先**算 `topicName := topicTitleSlug(text)`，直接作为 `CreateForumTopic` 的初始 `Name` 一步创建；删除整段 `EditForumTopic` 逻辑。
- **边界兜底**：`topicTitleSlug` 对空串或纯非字母数字输入（如纯 emoji/标点）返回 `""`——此时回退到 `newTopicPlaceholderName`。占位符常量因此保留（仍被此 fallback 与 dispatch 侧 `CreateTaskTopic` 引用，非死代码）。

**身份不变量核对（Cardinal Invariant）**：Topic 标题始终是**装饰性**的，路由身份走 `TopicID`/session-key/ledger（本信正文 Observations 已确证）。一步命名只改「首次显示什么标题」，不触碰任何持久绑定；`topicOwnershipRecorder` 仍在创建后立即以 `MessageThreadID`（稳定键）登记属主，位置不变。✓

**验证**：`go test ./platform/telegram/... -count=1` → `ok`；`go vet ./platform/telegram/` clean。测试断言更新为「以 slug `build-the-thing` 直接创建、`EditForumTopic` 调用数 = 0」。

**状态同前**：worktree `letter/L-0674` HEAD 现为 `1d257d0a`；未 push、未 restart daemon。
